#include <stdio.h>      // printf 함수를 사용하기 위해 필요
#include <string.h>     // strlen, strcpy 등을 사용하기 위해 필요
#include <math.h>       // sqrt, pow 등을 사용하기 위해 필요

int main() {
    printf("Hello");           // stdio.h에서 제공
    strlen("문자열");          // string.h에서 제공  
    sqrt(16);                 // math.h에서 제공
}